{
  "name": "@riftbound/client",
  "version": "0.4.4",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "tsc -b && vite build",
    "typecheck": "tsc -b --pretty false",
    "test": "tsx --tsconfig tsconfig.app.json --test tests/**/*.test.ts"
  },
  "dependencies": {
    "@riftbound/shared": "*",
    "@vitejs/plugin-react": "latest",
    "vite": "latest",
    "react": "latest",
    "react-dom": "latest"
  },
  "devDependencies": {
    "@types/react": "latest",
    "@types/react-dom": "latest",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
